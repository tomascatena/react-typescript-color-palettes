name: deploy-app

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - '.github/workflows/**'

defaults:
  run:
    working-directory: src

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build
